#parse("copyright.vm")
package ${plugin.baseDAOPackage};

import org.apache.log4j.Logger;

/**
 * Base interface for all data access objects.
 */
public interface BaseDAO {
    /** Logger */
    public static final Logger logger = Logger.getLogger(BaseDAO.class);

}
